"""Context Virtual Memory — version-bound, budget-enforced, process-isolated,
replayable, inspectable mechanism for materializing working sets.

This is L3 (System Service) and must not be imported by the microkernel.
"""
